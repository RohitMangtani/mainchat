# Hive Learnings

Lessons captured from past sessions. Every agent in this project reads this file.

- [2026-06-09] Kick API (kick.com/api/v2/channels/{slug}) sends permissive CORS but Cloudflare 403s all server-side fetches (node/curl TLS fingerprint). Fetch it DIRECTLY FROM THE BROWSER (works, returns chatroom.id for the Pusher chat socket wss://ws-us2.pusher.com/app/32cbd69e4b950bf97679); keep a server proxy only as fallback.
- [2026-06-09] Twitch public web GQL Client-ID kimne78kx3ncx6brgo4mv6wki5h0ko is DEAD (400 invalid header, 2026). For no-credential viewer counts use decapi.me/twitch/viewercount/{ch} (returns 200 with error PROSE — classify body text, only trust pure digits or "is offline"). Official path: Helix with TWITCH_CLIENT_ID/SECRET client_credentials.
- [2026-06-09] Brand research without browser access: YouTube channel HTML gives avatar (yt3.ggpht) + banner + videoIds; i.ytimg.com/sb storyboard spec URLs yield actual stream overlay frames; api.fxtwitter.com/{user} returns X profile bio/avatar/banner JSON when x.com blocks fetches; pbs.twimg.com/profile_banners/{id}/{ts}/1500x500 gets full banner. Sample hex with Pillow in a $TMPDIR venv.
- [2026-06-09] [2026-06-09] No-key emote APIs verified shapes: 7TV global 7tv.io/v3/emote-sets/global (.emotes[].data.host.url is protocol-relative, append /2x.webp), 7TV channel /v3/users/twitch/{id} (.emote_set.emotes), BTTV cached endpoints ([].id/.code -> cdn.betterttv.net/emote/{id}/2x), FFZ sets[*].emoticons[].urls (also protocol-relative). Resolve twitch login->id via api.ivr.fi/v2/twitch/user?login= (returns array, id may be string OR number). FFZ channel route uses LOGIN not id, so it loads even when ivr.fi fails. Twitch IRC emotes tag indices are by unicode CODE POINT (Array.from), inclusive end. Kick inline: [emote:{id}:{name}] -> files.kick.com/emotes/{id}/fullsize. lib/emotes.ts EmoteResolver handles all of this.
- [2026-06-09] [2026-06-09] Keyless X live-broadcast chat: guest token (POST api.twitter.com/1.1/guest/activate.json with the public web bearer AAAA...ANRILg...) -> user id (graphql UserByScreenName G3KGOASz96M-Qu0nwmGXNg, falls back to cdn.syndication.twimg.com followbutton info.json then api.fxtwitter.com/{user}) -> 1.1/broadcasts/by_user_id/{id}.json (state RUNNING) -> 1.1/live_video_stream/status/{bid}.json gives chatToken -> POST proxsee.pscp.tv/api/v2/accessChatPublic {chat_token} gives {endpoint,access_token,room_id}. WS = endpoint https->wss + /chatapi/v1/chatnow; on open send kind:3 auth frame then kind:2 join frame; chat arrives as kind:1 with triple-nested JSON strings (frame.payload -> payload.body -> body.body text, type 1 = chat). Anon feed throttled ~3-5s. Implemented in app/api/x/live/route.ts + lib/connectors/xlive.ts.
